schema_version: 1

context:
  version: "0.1.1"

package:
  name: tree-sitter-muttrc
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/t/tree-sitter-muttrc/tree_sitter_muttrc-${{ version }}.tar.gz
  sha256: 81bee42299c7569b85dad1e36cf2c3b6a7985255d1860936f9b3a7859f45ea38

build:
  number: 0
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ stdlib('c') }}
    - if: build_platform != target_platform
      then:
        - cross-python_${{ target_platform }}
        - python
  host:
    - python
    - setuptools >=42
    - wheel
    - pip
  run:
    - python

tests:
  - python:
      imports:
        - tree_sitter_muttrc
  - requirements:
      run:
        - pip
        - python
    script:
      - pip check

about:
  summary: Muttrc grammar for tree-sitter
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/neomutt/tree-sitter-muttrc

extra:
  recipe-maintainers:
    - danielnachun
